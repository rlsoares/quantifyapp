
<nav class="navbar navbar-inverse navbar-static-top">

  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbarsExampleDefault" name="button">

        <span class="sr-only"> Toggle-navigation </span>
        <span class="icon-bar"> </span>
        <span class="icon-bar"> </span>
        <span class="icon-bar"> </span>

      </button>
      <% if !user_sign_in? %>
        <%= link_to 'Quantify', root_url, class: 'navbar-brand'%>
      <% else %>
        <%= link_to 'Quantify', users_path + '/' + current_user.id.to_s, class: 'navbar-brand'%>
      <% end %>
    </div>

    <div class="collapse navbar-collapse" id="expanded-menu">

        <ul class="nav navbar-nav">
          <% if !user_sign_in? %>
          <li class="active">
              <%=link_to 'Nova Conta', new_user_path%>
          </li>
          <% end %>
        </ul>
        <div class="nav navbar-nav navbar-right">
          <% if !user_sign_in? %>
              <li>
                <%= link_to 'Entrar', entrar_path%>
              </li>
          <% else %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                  Bem vindo, <%= current_user.nome %> <span class="caret"></span>
                </a>
                <ul class="dropdown-menu">
                  <li>
                    <%= link_to 'Dashboard', users_path + '/' + current_user.id.to_s  %>
                  </li>
                  <li>
                    <%= link_to 'Minha Tabela', users_path + '/' + current_user.id.to_s + '/tables' %>
                  </li>
                  <li>
                    <%= link_to 'Editar', users_path + '/' + current_user.id.to_s + '/edit' %>
                  </li>
                  <li class="divider" role="separator"></li>
                  <li>
                    <%= link_to 'Sair', sair_path, method: :delete %>
                  </li>
                </ul>
              </li>
          <% end %>
        </div>
    </div>



  </div>




</nav>
